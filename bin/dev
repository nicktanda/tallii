#!/usr/bin/env sh

if ! gem list foreman -i --silent; then
  echo "Installing foreman gem"
  gem install foreman
fi

if ! gem list foreman -i --silent; then
  echo "Installing foreman gem"
  gem install overmind
fi

exec overmind start -f Procfile "$@"
